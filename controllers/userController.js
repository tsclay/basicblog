const express = require('express')
const User = require('../models/User')

const user = express.Router()

// Get user data
user.get('/', (req, res) => {
  //
})

// Create new user
user.post('/', (req, res) => {
  //
})

// Update user details
user.put('/', (req, res) => {
  //
})

// Delete user account
user.delete('/', (req, res) => {
  //
})
